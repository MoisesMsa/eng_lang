def main() -> void {
    x: float = 3.5;   // Número racional
    y: float = 2.1;   // Número racional
    c: int = 5;       // Número inteiro

    resultado: float = (x * x) - y + c;  // Avaliação da expressão

    print(resultado); // Impressão do resultado
}
